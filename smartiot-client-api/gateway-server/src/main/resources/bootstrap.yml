spring:
  application:
    name: gateway-server
  cloud:
    nacos:
      config:
        server-addr: 47.113.216.179:8848
        file-extension: yml
        namespace: dev
        timeout: 5000
        config-long-poll-timeout: 1000
        config-retry-time: 10000
        max-retry: 3
        refresh-enabled: true
        enable-remote-sync-config: true